% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusion.R
\name{skald_fuse_ranks}
\alias{skald_fuse_ranks}
\title{Fuse ranking signals}
\usage{
skald_fuse_ranks(
  .data,
  ...,
  method = c("rrf", "weighted_sum"),
  k = 60,
  weights = NULL,
  rank_col = "fused_rank",
  score_col = "fused_score"
)
}
\arguments{
\item{.data}{A data frame or tibble.}

\item{...}{<\code{\link[tidyselect:language]{tidy-select}}> Rank or score columns to
fuse.}

\item{method}{Fusion method, either \code{"rrf"} or \code{"weighted_sum"}.}

\item{k}{Reciprocal-rank fusion constant.}

\item{weights}{Optional numeric weights for selected columns.}

\item{rank_col, score_col}{Names for output rank and score columns.}
}
\value{
A tibble ordered by fused score, with fused score and rank columns.
}
\description{
Combines multiple rank or score columns using reciprocal-rank fusion or a
weighted normalized score sum.
}
\examples{
results <- tibble::tibble(doc_id = 1:3, bm25_rank = c(1, 3, 2), late_score = c(0.2, 0.9, 0.6))
skald_fuse_ranks(results, bm25_rank, late_score)
}
