% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/encode.R
\name{skald_encode_col}
\alias{skald_encode_col}
\title{Encode a data-frame column}
\usage{
skald_encode_col(
  .data,
  model,
  text,
  kind = c("document", "query"),
  name = ".skald_embedding",
  batch_size = 32,
  ...
)
}
\arguments{
\item{.data}{A data frame or tibble.}

\item{model}{A \link{SkaldModel}.}

\item{text}{<\code{\link[tidyselect:language]{tidy-select}}> Column containing text to
encode.}

\item{kind}{Embedding kind, either \code{"document"} or \code{"query"}.}

\item{name}{Name of the output list-column. Use \code{NULL} to return only the
\link{SkaldEmbeddings} object.}

\item{batch_size}{Encoding batch size.}

\item{...}{Additional arguments passed to \code{\link[=skald_encode_queries]{skald_encode_queries()}} or
\code{\link[=skald_encode_documents]{skald_encode_documents()}}.}
}
\value{
A tibble with an embedding list-column, or a \link{SkaldEmbeddings} object
when \code{name = NULL}.
}
\description{
Encodes one text column and either appends a list-column of opaque Python
embeddings or returns a \link{SkaldEmbeddings} object directly.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
docs <- tibble::tibble(text = "Use filter() to keep rows.")
skald_encode_col(docs, model, text)
\dontshow{\}) # examplesIf}
}
