% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rerank.R
\name{skald_rerank}
\alias{skald_rerank}
\title{Rerank candidate documents with a ColBERT model}
\usage{
skald_rerank(
  .data,
  model,
  query,
  text,
  id = NULL,
  query_id = NULL,
  top_k = NULL,
  batch_size = 32,
  device = NULL,
  score_col = "late_score",
  rank_col = "late_rank",
  keep_all = TRUE,
  ...
)
}
\arguments{
\item{.data}{A data frame or tibble of candidate documents.}

\item{model}{A \link{SkaldModel}.}

\item{query}{Query string, or a column that resolves to one query string per
group.}

\item{text}{<\code{\link[tidyselect:language]{tidy-select}}> Text column to rerank.}

\item{id}{Optional <\code{\link[tidyselect:language]{tidy-select}}> column or columns
that identify candidate rows.}

\item{query_id}{Optional <\code{\link[tidyselect:language]{tidy-select}}> column or
columns that identify query groups.}

\item{top_k}{Optional number of highest-ranked rows to keep per group.}

\item{batch_size}{Encoding batch size.}

\item{device}{Optional torch device string used during reranking.}

\item{score_col, rank_col}{Names for the output score and rank columns.}

\item{keep_all}{Whether to keep rows that are not returned by the reranker.}

\item{...}{Additional arguments passed to the PyLate \code{encode()} method.}
}
\value{
A tibble with the original columns plus late-interaction score and
rank columns.
}
\description{
Reranks a data frame of first-stage candidate documents by late-interaction
MaxSim score.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
candidates <- tibble::tibble(doc_id = "a", text = "Use filter() to keep rows.")
skald_rerank(candidates, model, query = "filter rows", text = text, id = doc_id)
\dontshow{\}) # examplesIf}
}
